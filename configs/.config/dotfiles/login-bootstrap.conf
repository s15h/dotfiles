# Supported login bootstrap entries:
#   gh|<hostname>|<bitwarden item with token in the password field>
#   docker|<registry>|<username>|<bitwarden item with password or token in the password field>
#
# Notes:
# - Keep only item names or IDs in this file. Do not store raw passwords or tokens here.
# - The preparation script will offer to run this bootstrap after the apps are installed.
#
# Examples:
# gh|github.com|github-cli-token
# docker|ghcr.io|s15h|github-cli-token
# docker|index.docker.io|s15h|docker-hub-password

gh|github.com|gh cli token
